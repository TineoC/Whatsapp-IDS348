/// <reference types="react" />
import { EmojiStyle } from '../../types/exposedTypes';
export declare function ExportedEmoji({ unified, size, emojiStyle, lazyLoad }: {
    unified: string;
    emojiStyle?: EmojiStyle;
    size?: number;
    lazyLoad?: boolean;
}): JSX.Element | null;
